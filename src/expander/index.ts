import { $ } from "../variables/index.js";

export function expand(word: string): string {
    // TODO: tilde expansion, parameter expansion, command substitution,
    // arithmetic expansion, word splitting, glob expansion, quote removal
    return word;
}
